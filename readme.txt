In order to run the game using eclipse, use the command gradlew genEclipseRuns. In the CreativeCore project you will find two launch configurations.
These will launch CreativeCore only, if you want to launch other mods alongside it, you can configure the run configuration and add the different projects to the classpath.
